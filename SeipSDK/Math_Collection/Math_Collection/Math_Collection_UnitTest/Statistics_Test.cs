using Math_Collection.LinearAlgebra.Matrices;
using Math_Collection.Statistic;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using System.Drawing;

namespace Math_Collection_UnitTest
{
    [TestClass]
    public class Statistics_Test
    {
        [TestMethod]
        public void LinearRegressionMatrix()
        {
            //List<Point> points = new List<Point>();
            //points.Add(new Point(1, 3));
            //points.Add(new Point(2, 1));
            //points.Add(new Point(3, 4));
            //points.Add(new Point(4, 6));
            //points.Add(new Point(5, 5));
            //Matrix m = LinearRegression.CreateRegression(points, 2);

            //double[,] vals = new double[,] { { 55 , 15 }, { 15, 5 }};
            //Matrix expected = new Matrix(vals);

            //Assert.AreEqual(expected, m);

        }

        [TestMethod]
        public void LinearRegressionVector()
        {
            //List<Point> points = new List<Point>();
            //points.Add(new Point(1, 3));
            //points.Add(new Point(2, 1));
            //points.Add(new Point(3, 4));
            //points.Add(new Point(4, 6));
            //points.Add(new Point(5, 5));
            //LinearRegression.CreateRegression(points, 2);
        }
    }
}
